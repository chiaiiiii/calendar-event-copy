# 要件定義書 — カレンダー予定コピー Chrome拡張

## 1. 概要

Googleカレンダーに登録された予定のタイトルを、指定日付で一覧取得しクリップボードにコピーするChrome拡張機能。日報作成時の手入力を省くことを目的とする。

---

## 2. 背景・目的

- 日報作成時、その日の予定を手動でカレンダーから転記する手間がある
- 予定タイトルを箇条書き形式で自動収集し、ワンクリックで貼り付けられるようにする

---

## 3. 機能要件

### 3-1. 予定タイトルの取得

| # | 要件 |
|---|------|
| F-01 | Googleカレンダー（`https://calendar.google.com/*`）の開いているタブからイベント情報を取得できること |
| F-02 | 取得対象はイベントの**タイトルのみ**（時刻・参加者・場所などの付帯情報は不要） |
| F-03 | 同日の重複タイトルは1件に集約すること |
| F-04 | 「勤務場所を追加」「場所を追加」「予定を追加」「タスクを追加」等のUI操作ボタンはイベントとみなさず除外すること |
| F-05 | カレンダーUIのナビゲーション要素（「戻る」「検索」「今日」等）は除外すること |
| F-06 | イベントのaria-labelは `午前9:30～午前10時、「FDEコセンスアップデート」、Ai Higuchi、...` の形式を想定し、時刻パターン（午前/午後＋時刻）を含むもののみをイベントとして扱うこと |
| F-07 | タイトルは `「」` で囲まれた部分を抽出すること |

### 3-2. ポップアップUI

| # | 要件 |
|---|------|
| F-08 | ポップアップには日付入力欄・「予定を取得」ボタン・結果表示エリア・「クリップボードにコピー」ボタンを表示すること |
| F-09 | 日付のデフォルト値は今日の日付とすること |
| F-10 | 取得結果は `・タイトル` の箇条書き形式で表示すること |
| F-11 | 予定が0件の場合は「この日の予定はありません」と表示すること |
| F-12 | 取得中はステータスに「取得中...」と表示すること |
| F-13 | コピー成功後はボタンテキストを「コピーしました！」に2秒間変更すること |

### 3-3. Content Scriptの注入

| # | 要件 |
|---|------|
| F-14 | 拡張機能再読み込み後にカレンダータブを再読み込みしなくても動作すること（`scripting.executeScript` による動的注入） |
| F-15 | Googleカレンダーのタブが見つからない場合はエラーメッセージを表示すること |

---

## 4. 非機能要件

| # | 要件 |
|---|------|
| NF-01 | Manifest V3 準拠であること |
| NF-02 | 必要な権限は `activeTab`・`scripting`・`clipboardWrite` および `https://calendar.google.com/*` のホスト権限のみとすること（最小権限の原則） |
| NF-03 | 外部サーバーへの通信は行わないこと（ローカル処理のみ） |
| NF-04 | Googleカレンダーの「日」表示モードでの動作を主要対象とすること |

---

## 5. ファイル構成

```
calendar-event-copy/
├── manifest.json   # 拡張機能の設定・権限定義
├── popup.html      # ポップアップのHTML
├── popup.js        # ポップアップのロジック（タブ検索・メッセージ送信・UI制御）
└── content.js      # カレンダーページ上でのDOM解析・タイトル抽出
```

---

## 6. イベント取得ロジック詳細

### 対象セレクター
```
[role="button"][aria-label], [data-eventchip], [data-eventid]
```

### フィルタリング条件
1. `aria-label` が存在すること
2. `aria-label` に時刻パターン（`/[午前午後]\d+[時:]\d*/`）が含まれること
3. UI要素キーワード（勤務場所を追加 等）が含まれないこと

### タイトル抽出優先順
1. `aria-label` 内の `「タイトル」` 形式（主パターン）
2. 時刻の後に続くテキスト（サブパターン）
3. 短い単体ラベル（60文字未満、数字始まりでない）

---

## 7. 既知の制約・注意事項

- Googleカレンダーのフロントエンドは予告なく変更される可能性があり、DOM構造の変更によりセレクターやaria-labelの形式が変わると動作しなくなる場合がある
- 「日」表示モード以外（週・月表示）での動作は保証しない
- 他ユーザーが主催する予定も同一カレンダー上に表示されている場合は取得対象に含まれる

---

## 8. 用語定義

| 用語 | 定義 |
|------|------|
| イベント | Googleカレンダーに登録された予定（会議・タスク等） |
| イベントチップ | カレンダー画面上に表示されるイベントのUI部品 |
| aria-label | アクセシビリティ用途でHTML要素に付与される説明テキスト。Googleカレンダーでは時刻・タイトル・参加者・場所等を含む |
